# Security Policy

Security fixes target the latest JavaScript, Python, and .NET SDK releases.
Report vulnerabilities privately to `security@postq.dev` with the package,
version, runtime, and reproduction. Never include a live API key, payload,
signature private material, or customer response data.